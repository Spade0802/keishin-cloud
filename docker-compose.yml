services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: keishin
      POSTGRES_PASSWORD: keishin_dev
      POSTGRES_DB: keishin_cloud
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
